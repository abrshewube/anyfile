{
  "name": "anyfile-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "AnyFile – unified, modular file handling toolkit.",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test"
  },
  "devDependencies": {
    "@types/node": "^20.10.4",
    "typescript": "^5.3.3",
    "tsup": "^7.2.0",
    "vitest": "^1.1.0"
  }
}

